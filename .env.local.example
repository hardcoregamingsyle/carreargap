# Copy to .env.local for local development (never committed).
#
# Value comes from your Convex deployment's dashboard (Settings > URL & Deploy
# Key), or derive it from the deploy key: prod:<name>|... -> https://<name>.convex.cloud
NEXT_PUBLIC_CONVEX_URL=https://fabulous-magpie-332.convex.cloud
